===============================================================
LINEUP - 15 MINUTTER POWERPOINT PRÆSENTATION
Fokus på Jimmi Larsens arbejde: Auth, Friends & Chat System
===============================================================

SLIDE 1: TITELSLIDE (0:30)
─────────────────────────
Titel: "LineUp - Social Platform for Musicians"
Undertitel: "Building the Friend & Chat System"
Dit navn og dato
Billede: LineUp logo eller hero image

---

SLIDE 2: PROJECT OVERVIEW (1:00)
───────────────────────────────
Hvad er LineUp?
  • Social media platform for musikere og musikproducere
  • Trovændig netværksplatform i nordisk musikindustri
  • Samarbejdsfinding, netværk og karrierevækst

Teknologi Stack:
  • Frontend: React 19 + React Router 7 + Tailwind CSS
  • Backend: Node.js + Express
  • Database: Supabase PostgreSQL
  • Auth: Supabase Auth (JWT tokens)

---

SLIDE 3: MIN ROLLE - SYSTEMARKITEKTUR (1:30)
──────────────────────────────────────────────
Jeg lavede tre hovedkomponenter:

1️⃣ Authentication Flow
   • Login/Register/GetStarted onboarding

2️⃣ Friends System  
   • Connections med friend requests (accept/decline)

3️⃣ Chat System
   • 1-til-1 chats OG gruppechats
   • Real-time messaging med Supabase Realtime

Arkitektur: Hybrid client-server
  • Frontend: Direkte Supabase connection + JWT auth
  • Backend: REST API med server-side validation

---

SLIDE 4: AUTHENTICATION - LOGIN/REGISTER (2:00)
────────────────────────────────────────────────
Login Flow:
  ✓ Email/Password validation (8+ tegn)
  ✓ Remember Me funktionalitet
  ✓ Supabase Auth JWT tokens
  ✓ Error handling med brugervenlig feedback

Register Flow:
  ✓ Email validering
  ✓ Password strength check
  ✓ Automatisk profil oprettelse i database
  ✓ Transition til GetStarted flow

Sikkerhed:
  • Password hashing via Supabase (bcrypt)
  • JWT tokens for session management
  • Protected routes med ProtectedRoute wrapper

---

SLIDE 5: GETSTARTED ONBOARDING (2:30)
──────────────────────────────────────
6-trin onboarding flow for nye brugere:

0. Carousel: Marketing slides (swipeable)
1. Signup: Account creation (email/password)
2. User Type: Musician vs Service Provider
3. User Info: Name, phone, birthdate, city
4. Interests: Musik genrer og interesser
5. Subscription: Plan selection

State Management:
  • Samler data gennem flere steps
  • Gemmer alt i localStorage PLUS database (profiles tabel)
  • Loading screen med redirect efter completion

Komponenter brugt:
  • Carousel.jsx, SignupForm.jsx, UserTypeSelect.jsx
  • UserInformation.jsx, Interests.jsx, Subscription.jsx

---

SLIDE 6: FRIENDS SYSTEM - CONNECTIONS (3:00)
──────────────────────────────────────────────
Database struktur (connections tabel):
┌─────────────────────────────────────┐
│ user_id_1 │ user_id_2 │ status      │
├─────────────────────────────────────┤
│ UUID      │ UUID      │ pending     │
│ UUID      │ UUID      │ accepted    │
│ UUID      │ UUID      │ rejected    │
└─────────────────────────────────────┘

Status system:
  🔴 pending   → Awaiting acceptance
  🟢 accepted  → Active connection (friends)
  ❌ rejected  → Declined request

Flow:
  1. User A sender friend request → status = 'pending'
  2. User B ser request i FriendsList
  3. User B kan ACCEPT eller DECLINE
  4. Status opdateres → connection etableret eller rejected

RLS (Row-Level Security):
  • KunUser B kan accepte/decline deres eget requests
  • Kun accepted connections vises i "Friends"
  • Sikrer privacy og integritet

---

SLIDE 7: FRIENDS UI - FRIENDSLIST (3:30)
──────────────────────────────────────────
FriendsList Component Features:

Interaktiv Design:
  ✓ Draggable horizontal scroll (touch-friendly)
  ✓ Click detection (drag distance tracking)
  ✓ Context menu ved click på venn

Funktioner:
  • Show Profile: Navigere til venns profil
  • Start Chat: Åbn chat med denne venn
  • Accept/Decline: Handle friend requests

UI/UX:
  • Avatar display
  • Responsive (mobile + desktop)
  • Smooth animations
  • Tailwind styling

---

SLIDE 8: ACCEPT/DECLINE FLOW (4:00)
────────────────────────────────────
User Experience:

1. User ser pending friend requests i feed
2. Accept button → 
   PATCH /api/connections/:id
   { status: 'accepted' }
   → RLS policy tjekker auth.uid() = user_id_2
   
3. Decline button →
   DELETE eller UPDATE status: 'rejected'

Database Operation:
  • Supabase RLS sikrer kun modtageren kan handle
  • Requester ID tracked i migrations/005
  • Status constraints på (pending|accepted|rejected)

After Action:
  ✓ Request fjernes fra pending
  ✓ Connection vises i Friends list
  ✓ Begge brugere kan nu chatte

Fejlhåndtering:
  • Validering på client & server
  • JWT token check på backend
  • User-friendly error messages

---

SLIDE 9: CHAT SYSTEM - DATABASE STRUKTUR (4:30)
────────────────────────────────────────────────
Database tabeller:

1. THREADS (Chat samtaler)
┌─────────────────────────────────┐
│ thread_id      │ UUID            │
│ thread_type    │ 'direct'/'group'│
│ thread_name    │ Group name      │
│ created_by     │ UUID            │
│ created_at     │ timestamp       │
└─────────────────────────────────┘

2. THREAD_PARTICIPANTS (Medlemmer)
┌──────────────────────────────────┐
│ thread_id  │ user_id │ joined_at │
└──────────────────────────────────┘

3. MESSAGES (Beskeder)
┌───────────────────────────────────┐
│ message_id      │ UUID             │
│ thread_id       │ UUID (FK)        │
│ sender_id       │ UUID (FK)        │
│ message_content │ text             │
│ created_at      │ timestamp        │
│ is_deleted      │ boolean (soft del)│
└───────────────────────────────────┘

Relationer:
  • thread_id (threads) → thread_id (thread_participants)
  • thread_id (threads) → thread_id (messages)
  • sender_id (messages) → id (profiles)

---

SLIDE 9B: RLS - ROW-LEVEL SECURITY (4:45)
──────────────────────────────────────────
Hvad er RLS?

RLS = Row-Level Security (Række-niveau sikkerhed)

Definition:
  RLS er en PostgreSQL-funktion der kontrollerer PRÆCIS HVILKE RÆKKER
  en bruger kan læse, indsætte, opdatere eller slette i en tabel.
  
  Det betyder: Ikke alle brugere kan se ALLE data - kun DATA DE EJER!

Eksempel uden RLS:
  ❌ User A kan se ALLE beskeder fra ALLE users
  ❌ User A kan slette andres beskeder
  ❌ Meget usikkert!

Eksempel MED RLS:
  ✅ User A kan KUN se egne beskeder
  ✅ User A kan KUN se chats de er medlem af
  ✅ User A kan IKKE se andres private data
  ✅ MEGET sikker!

RLS Policies vi bruger:

1. Messages Table:
   "Users can only see messages in their threads"
   → auth.uid() = ANY(thread participants)
   
2. Connections Table:
   "Users can only see own connections"
   → auth.uid() = user_id_1 OR auth.uid() = user_id_2
   
3. Threads Table:
   "Users can only see threads they're in"
   → auth.uid() = ANY(participant list)

Hvordan virker det?
  1. User logger ind → JWT token med user ID
  2. Query til database → RLS checker: "Er dette user autoriseret?"
  3. Hvis JA → Data returneres
  4. Hvis NEJ → Error 403 (Forbidden)

Fordele:
  ✓ Sikkerhed på DATABASE-niveau (ikke kun app-niveau!)
  ✓ Selv hvis app har bug → data er stadig sikret
  ✓ Scalable (virker for millioner af rows)
  ✓ Performance-friendly (filtrering på DB)

---

SLIDE 11: 1-til-1 CHAT FLOW (5:00)
───────────────────────────────────
One-to-One Chat:

1. User A åbner NewChatPage
2. Vælger User B fra Friends list
3. Klik "Start Chat"
   → Checker om thread eksisterer
   → CREATE thread hvis ikke
   → thread_type = 'direct'

4. Naviger til OneToOneChatPage
   → Load messages fra threads/:threadId
   → Real-time subscription via Supabase

5. Type message + Send
   → INSERT i messages tabel
   → Realtime push til modtager
   → Message vises instantly

UI Components:
  • OneToOneChatPage.jsx
  • Chat.jsx (Message component)
  • ChatInput.jsx (Send logic)

---

SLIDE 12: GRUPPECHAT FLOW (5:30)
─────────────────────────────────
Group Chat - Nye Features:

1. User åbner GroupChatPage
2. Vælger FLERE venner (multi-select)
3. Optional: Tast group name
4. Klik "Create Group"
   → CREATE thread med thread_type = 'group'
   → thread_name = "Kitchen Talk" (f.eks.)
   → INSERT multiple rows i thread_participants
   → Alle members → joined_at

5. Gruppe chat åbnes
   → Viser alle members
   → Viser group name i header
   → Same message flow som 1-til-1

Forskelle fra 1-til-1:
  ✓ Multiple participants
  ✓ Group name display
  ✓ Member list visibility
  ✓ thread_type = 'group'

---

SLIDE 13: REAL-TIME MESSAGING (6:00)
─────────────────────────────────────
Real-Time Features via Supabase:

Realtime Subscriptions:
  • Subscribe til messages i specifik thread
  • Listen for INSERT, UPDATE, DELETE
  • Auto-push til andre members

Flow:
  1. Component mounter
     → supabase
        .from('messages')
        .on('INSERT', {...})
        .subscribe()

  2. User A sender besked
     → INSERT i messages
     → Supabase broadcasts
     → User B får instant notification

  3. Messages tabel subscribe:
     • Viser nye beskeder live
     • Typing indicators
     • Read receipts (future)

Websocket Connection:
  • Persistent connection
  • Low-latency delivery
  • Automatic reconnection

---

SLIDE 14: MIGRATION TIMELINE (6:30)
────────────────────────────────────
Database Evolution - 18 Migrations:

001-003: Core structure (messages, threads, participants)
004-005: RLS policies + Connection status system
006-012: Thread/Message RLS fixes + Realtime enablement
013-018: Group naming, collaboration fixes, Spotify URLs

Key Migrations I handled:
  ✓ 001: Fix chat structure (threads/messages/participants)
  ✓ 005: Add connection status (pending/accepted/rejected)
  ✓ 008: Fix threads RLS policies
  ✓ 010: Fix thread_id default (UUID generation)
  ✓ 013: Add group_name to threads

RLS (Row-Level Security):
  • Sikrer users kan KUN se egne chats
  • Kan KUN sende beskeder i threads de er member af
  • Preventing unauthorized access

---

SLIDE 15: BACKEND API INTEGRATION (7:00)
──────────────────────────────────────────
REST API Endpoints (Express):

GET /api/threads                    → Get all threads for user
GET /api/threads/:threadId          → Get thread + participants
POST /api/threads                   → Create thread
POST /api/threads/:threadId/members → Add member to group

GET /api/messages/:threadId         → Get messages
POST /api/messages                  → Send message
DELETE /api/messages/:messageId     → Delete message

GET /api/connections                → Get friends
POST /api/connections               → Send friend request
PATCH /api/connections/:id          → Accept/Decline

GET /api/profiles/:userId           → Get user profile
PUT /api/profiles                   → Update profile

Authentication:
  • JWT Bearer token validation
  • middleware/auth.js checks headers
  • Server-side RLS enforcement

---

SLIDE 16: FRONTEND ROUTING (7:30)
──────────────────────────────────
React Router v7 Structure:

Public Routes:
  /get-started    → GetStarted (onboarding)
  /login          → Login flow
  /register       → Register flow

Protected Routes (kræver auth):
  /                      → Home (feed)
  /profile/:userId       → User profile
  /edit-profile          → Edit my profile
  /chat                  → ChatList
  /chat/new              → NewChatPage (1-til-1)
  /chat/group            → GroupChatPage
  /chat/:threadId        → OneToOneChatPage

Komponenter:
  • PublicLayout.jsx      → Ulogged users
  • ProtectedLayout.jsx   → Logged in users
  • Root.jsx              → Main nav + sidebar

---

SLIDE 17: STATE MANAGEMENT (8:00)
──────────────────────────────────
How State is Managed:

React Context:
  • AuthContext.jsx → Global auth state
  • useAuth() hook → Access current user

Component State (useState):
  • Local chat messages
  • Input fields
  • UI toggles
  • Loading states

Custom Hooks:
  • useChat.js → Chat-specific logic
  • useProfile.js → Profile data
  • useImageUpload.js → File uploads

Database State:
  • Supabase as "source of truth"
  • Real-time subscriptions
  • localStorage for UX (remember me, etc.)

---

SLIDE 18: SECURITY FEATURES (8:30)
───────────────────────────────────
How We Protect Users:

1. Authentication:
   ✓ Supabase Auth (industry standard)
   ✓ Password hashing (bcrypt)
   ✓ JWT tokens with expiration

2. Authorization:
   ✓ RLS policies on every table
   ✓ Server-side token validation
   ✓ Users can ONLY access own data

3. Database:
   ✓ Foreign key constraints
   ✓ Status validation (CHECK constraints)
   ✓ Soft deletes (is_deleted flag)

4. API:
   ✓ JWT header validation
   ✓ CORS configuration
   ✓ Rate limiting (future)

Example - Friends Request:
  • Kun requester kan se pending requests
  • Kun recipient kan accept/decline
  • Rejected requests stay in DB (audit trail)

---

SLIDE 19: CHALLENGES & SOLUTIONS (9:00)
────────────────────────────────────────
Problemer vi løste:

❌ Problem 1: RLS policies too strict
   ✓ Solution: Fine-tuned policies per operation

❌ Problem 2: Group chat participants not loading
   ✓ Solution: Proper JOIN queries + thread_participants RLS

❌ Problem 3: Message content column mismatch
   ✓ Solution: Migration 001 fixed column naming

❌ Problem 4: Friend requests not persisting
   ✓ Solution: Added status column + constraints

❌ Problem 5: Real-time subscriptions failing
   ✓ Solution: Enabled realtime on tables (Migration 012)

Key Learning:
  • PostgreSQL RLS requires careful planning
  • Realtime needs explicit enablement
  • Client-server coordination is critical

---

SLIDE 20: PERFORMANCE & OPTIMIZATION (9:30)
─────────────────────────────────────────────
Optimizations:

Database:
  ✓ Indexes on frequently queried columns
    - idx_connections_status
    - idx_connections_requester
  ✓ Pagination for large message lists
  ✓ Eager loading with JOINs

Frontend:
  ✓ Component memoization (React.memo)
  ✓ Lazy loading routes
  ✓ Debouncing on search/typing

Real-time:
  ✓ Selective subscriptions (only current thread)
  ✓ Unsubscribe on unmount
  ✓ Efficient re-renders

---

SLIDE 21: FUTURE IMPROVEMENTS (10:00)
──────────────────────────────────────
What We Could Add:

Chat Features:
  • Read receipts
  • Typing indicators (UI ready, backend partial)
  • Message reactions (emojis)
  • File sharing in chat
  • Voice messages
  • Call integration (WebRTC)

Social:
  • Block users
  • Mute conversations
  • Pinned messages
  • Message search

Notifications:
  • Push notifications
  • Email digests
  • @mentions in groups
  • Activity feed

---

SLIDE 22: TECH HIGHLIGHTS (10:30)
──────────────────────────────────
Stack & Patterns:

Frontend:
  • Modern React 19 with hooks
  • React Router 7 (SSR + HMR)
  • Tailwind CSS (utility-first)
  • Real-time with Supabase
  • TypeScript (type safety)

Backend:
  • Express.js (lightweight)
  • PostgreSQL (relational data)
  • JWT authentication
  • RLS (security at DB level)

DevOps:
  • Git version control
  • 18 database migrations
  • Render deployment
  • Environment variables

---

SLIDE 23: TESTING & VERIFICATION (11:00)
──────────────────────────────────────────
How We Tested:

Manual Testing:
  ✓ Login/Register flow end-to-end
  ✓ Friend request acceptance
  ✓ 1-til-1 chat messages
  ✓ Group chat creation
  ✓ Real-time message delivery
  ✓ Permissions (RLS enforcement)

Test Data:
  • Test user: games@designbyjimmi.com
  • Multiple test accounts created
  • Group chats with 3+ members

Debugging:
  • Console logging
  • Supabase logs
  • Network tab (browser DevTools)
  • Database direct queries

---

SLIDE 24: DEPLOYMENT (11:30)
──────────────────────────────
Live on Render:

Frontend (React SPA):
  https://sem-proj-mojah-web-consulting-frontend.onrender.com/

Backend (Express API):
  https://sem-proj-mojah-web-consulting-backend.onrender.com

Database (Supabase):
  • PostgreSQL hosted
  • Realtime enabled
  • 18 migrations applied
  • RLS policies active

Git Workflow:
  • dev branch → development
  • main branch → production
  • PR reviews (team)
  • Automated deployments

---

SLIDE 25: SUMMARY - MY CONTRIBUTION (12:00)
─────────────────────────────────────────────
What I Built:

✅ Complete Authentication System
   • Login/Register/GetStarted flow
   • Onboarding with 6 steps
   • Profile completion

✅ Friends System
   • Friend requests with accept/decline
   • Status tracking (pending/accepted/rejected)
   • FriendsList UI with drag & context menu

✅ Chat System
   • 1-til-1 direct messaging
   • Group chat with multiple members
   • Real-time messaging
   • Thread management

✅ Database Architecture
   • 18 migrations
   • RLS security policies
   • Proper schema design
   • Real-time subscriptions

✅ Backend API
   • Express routes for auth, connections, messages, threads
   • JWT validation
   • Error handling

Files Created/Modified:
  • 50+ components & routes
  • 18 SQL migrations
  • 10+ API endpoints
  • 100+ hours of development

---

SLIDE 26: KEY FILES REFERENCE (12:30)
──────────────────────────────────────
Important Files for Review:

Frontend Routes:
  • frontend/routes/Login.jsx
  • frontend/routes/Register.jsx
  • frontend/routes/GetStarted.jsx
  • frontend/routes/ChatList.jsx
  • frontend/routes/GroupChatPage.jsx
  • frontend/routes/OneToOneChatPage.jsx

Components:
  • frontend/components/FriendsList.jsx
  • frontend/components/Chat.jsx
  • frontend/contexts/AuthContext.jsx

Backend:
  • backend/routes/auth.js
  • backend/routes/connections.js
  • backend/routes/messages.js
  • backend/routes/threads.js

Database:
  • backend/migrations/001_fix_chat_structure.sql
  • backend/migrations/005_add_connection_status.sql
  • backend/migrations/013_add_group_name_to_threads.sql

---

SLIDE 27: THANK YOU (13:00)
────────────────────────────
Questions & Discussion

Key Takeaways:
  • Built production-ready chat system
  • Implemented secure authentication
  • Designed scalable database architecture
  • Real-time messaging at scale
  • Team collaboration framework

Contact:
  • GitHub: Saliandros/Sem1-LineUp
  • Email: games@designbyjimmi.com

Thank you for listening!

===============================================================
NOTES FOR PRESENTATION:
===============================================================

Timing Breakdown (15 minutes):
  Slides 1-2:   30 sec (intro + project overview)
  Slides 3-8:   2 min (my work + auth)
  Slides 9-14:  2 min (RLS + database + chat)
  Slides 15-19: 2 min (API + security)
  Slides 20-21: 1 min (optimization + future)
  Slides 22-23: 1 min (tech + testing)
  Slides 24-27: 1 min (deployment + summary)

Tips:
  • Use lots of visuals/diagrams (especially database)
  • Live demo if possible (show chat working)
  • Emphasize the complexity of RLS & real-time
  • Show before/after for friend requests
  • Mention team context (your role vs others)
  • Practice transitions between slides
  • Have backup screenshots ready

Demo Ideas:
  • Login with test account
  • Send friend request
  • Accept friend request
  • Send message in 1-til-1 chat
  • Create group chat
  • Show message appearing in real-time
  • Show database schema

Visual Assets Needed:
  ✓ LineUp logo
  ✓ Architecture diagram
  ✓ Database schema (already have)
  ✓ Flow diagrams (friend requests, chat)
  ✓ Screenshots of UI
  ✓ Code snippets (key functions)
  ✓ Timeline (18 migrations)

===============================================================
